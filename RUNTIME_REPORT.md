# تقرير تشغيل NIVARO

تاريخ الفحص: 15 يوليو 2026 — توقيت القاهرة

## حالة الخدمات

| الخدمة | العنوان | الحالة |
|---|---|---|
| المتجر | http://127.0.0.1:3000 | يعمل — HTTP 200 |
| لوحة الإدارة | http://127.0.0.1:3001 | تعمل — HTTP 200 |
| PostgreSQL | localhost:55432 | الحاوية تعمل وحالتها Healthy |
| Redis | localhost:6379 | الحاوية تعمل |
| API | http://127.0.0.1:4000/api | يعمل — HTTP 200 ومتصل بقاعدة البيانات |

## نتائج الجودة

- TypeScript: ناجح في المتجر ولوحة الإدارة والـAPI، دون أخطاء.
- Unit tests: ملف اختبار واحد، اختباران ناجحان، دون إخفاقات.
- Storefront production build: ناجح.
- Admin production build: ناجح.
- Prisma Client generation: ناجح.

## أحجام بناء الإنتاج

### المتجر

- الصفحة الرئيسية: 51.3 kB.
- First Load JS: 154 kB.
- JavaScript المشترك: 103 kB.
- تم توليد الصفحة الرئيسية و404 وrobots.txt وsitemap.xml كمحتوى ثابت.

### لوحة الإدارة

- الصفحة الرئيسية: 4.23 kB.
- First Load JS: 107 kB.
- JavaScript المشترك: 102 kB.

## قياسات الاستجابة المحلية

هذه القياسات هي لأول طلب في وضع التطوير وتشمل وقت التجميع عند الطلب:

- المتجر: HTTP 200، نحو 7.05 ثانية، HTML بحجم 43,418 بايت.
- لوحة الإدارة: HTTP 200، نحو 4.90 ثانية، HTML بحجم 21,922 بايت.
- robots.txt: HTTP 200.
- sitemap.xml: HTTP 200.

وضع الإنتاج المبني أسرع من أرقام التطوير السابقة بعد تشغيل `next start`.

## لوحة الإدارة الحية

- تم حل تعارض PostgreSQL بنقله إلى المنفذ 55432.
- تم إنشاء الجداول وتحميل 20 منتجًا حقيقيًا في قاعدة البيانات.
- لوحة الإدارة تقرأ الإحصاءات والمنتجات والطلبات والمخزون من API مباشرة.
- يمكن إضافة المنتجات وتعديل السعر والكمية والحالة وحذف المنتجات غير المرتبطة بطلبات.
- يمكن تغيير حالة الطلب وحفظ ألوان الهوية في PostgreSQL.
- أضيفت ثلاثة طلبات تجريبية دائمة وعميل تجريبي لتجربة التقارير وتدفق إدارة الطلبات.

## ملاحظات

- تم فتح المتجر ولوحة الإدارة في المتصفح.
- عمليات التطوير للواجهتين ما زالت تعمل في الخلفية.
- سجلات التشغيل موجودة في `runtime-logs/`.
